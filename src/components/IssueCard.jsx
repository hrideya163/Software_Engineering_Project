import { ArrowUpRight, Clock, Target } from 'lucide-react'; import { Link, useParams } from 'react-router-dom'; import DifficultyBadge from './DifficultyBadge'; import SkillTag from './SkillTag'
export default function IssueCard({issue}) { const {owner,repo}=useParams(); return <article className="card p-5"><div className="flex items-start justify-between gap-3"><div><p className="mono text-xs text-slate-500">#{issue.id}</p><h3 className="mt-1 font-bold text-ink">{issue.title}</h3></div><DifficultyBadge level={issue.difficulty}/></div><p className="mt-2 text-sm text-slate-600">{issue.summary}</p><div className="mt-4 flex flex-wrap gap-2">{issue.labels.map(x=><SkillTag key={x}>{x}</SkillTag>)}</div><div className="mt-5 flex items-center justify-between border-t pt-4 text-xs font-bold text-slate-500"><span className="flex items-center gap-1"><Target size={14} className="text-mint"/>{issue.match}% match</span><span className="flex items-center gap-1"><Clock size={14}/>{issue.effort}</span><Link to={`/repository/${owner}/${repo}/issues/${issue.id}`} className="inline-flex items-center gap-1 text-ink hover:text-blue-700">Analyze <ArrowUpRight size={14}/></Link></div></article> }
