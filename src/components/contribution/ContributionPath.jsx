import { Check, ExternalLink, X } from 'lucide-react'; import { useState } from 'react'
const steps=['Understand the issue','Read relevant documentation','Explore affected files/modules','Study similar PR','Learn required skills/concepts','Start implementation','Run tests','Submit pull request']
export default function ContributionPath({onClose}) { const [done,setDone]=useState([]); const toggle=i=>setDone(d=>d.includes(i)?d.filter(x=>x!==i):[...d,i]); return <section className="card overflow-hidden"><div className="flex items-center justify-between border-b bg-[#f8fffd] p-5"><div><p className="eyebrow text-emerald-700">Guided workflow</p><h2 className="mt-1 text-xl font-extrabold">Your contribution path</h2></div><button className="rounded-lg p-2 hover:bg-slate-100" onClick={onClose}><X size={19}/></button></div><div className="p-5"><div className="mb-5 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-mint transition-all" style={{width:`${done.length/steps.length*100}%`}}/></div><p className="mb-5 text-sm text-slate-600">{done.length} of {steps.length} steps complete. Move at your own pace.</p><ol className="space-y-3">{steps.map((step,i)=><li key={step} className="flex items-center gap-3"><button onClick={()=>toggle(i)} className={`grid h-7 w-7 shrink-0 place-items-center rounded-full border text-xs font-bold ${done.includes(i)?'border-mint bg-mint text-ink':'border-slate-300 text-slate-500'}`}>{done.includes(i)?<Check size={15}/>:i+1}</button><span className={`text-sm font-semibold ${done.includes(i)?'text-slate-400 line-through':'text-ink'}`}>{step}</span>{i<4&&<a href="#resources" className="ml-auto text-xs font-bold text-blue-700">Resource <ExternalLink className="inline" size={12}/></a>}</li>)}</ol></div></section> }
