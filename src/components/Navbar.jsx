import { Compass, Github, Menu, X } from 'lucide-react'; import { Link, NavLink } from 'react-router-dom'; import { useState } from 'react'
export default function Navbar() { const [open,setOpen] = useState(false); const links=[['How It Works','/how-it-works'],['Features','/#features'],['About','/about']]; return <header className="border-b border-slate-200 bg-white/90 backdrop-blur"><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link to="/" className="flex items-center gap-2 font-extrabold text-ink"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-mint"><Compass size={20}/></span>ContriMap</Link><div className="hidden items-center gap-7 md:flex">{links.map(([name,to])=><NavLink key={name} to={to} className="text-sm font-semibold text-slate-600 hover:text-ink">{name}</NavLink>)}<Link className="btn-primary py-2.5" to="/#analyze">Analyze Repository <Github size={16}/></Link></div><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></nav>{open&&<div className="border-t px-5 pb-4 md:hidden">{links.map(([name,to])=><Link key={name} to={to} className="block py-3 text-sm font-semibold">{name}</Link>)}</div>}</header> }
