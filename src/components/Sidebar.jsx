import { BarChart3, GitPullRequest } from 'lucide-react'; import { NavLink, useParams } from 'react-router-dom'
export default function Sidebar() { const {owner,repo}=useParams(); const base=`/repository/${owner}/${repo}`; const links=[['Dashboard',base,BarChart3],['Issues',`${base}/issues`,GitPullRequest]]; return <aside className="hidden w-60 shrink-0 border-r border-slate-200 bg-white p-4 lg:block"><p className="eyebrow mb-4 px-3">Model outputs</p>{links.map(([label,to,Icon])=><NavLink end={label==='Dashboard'} key={label} to={to} className={({isActive})=>`mb-1 flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-bold ${isActive?'bg-sky text-ink':'text-slate-500 hover:bg-slate-50'}`}><Icon size={18}/>{label}</NavLink>)}</aside> }
