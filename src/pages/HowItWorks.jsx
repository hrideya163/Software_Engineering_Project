import { ArrowDown, BrainCircuit, Database, GitBranch, Map, Route, Sparkles } from 'lucide-react'; import Navbar from '../components/Navbar'
const steps=[['GitHub Repository',GitBranch,'Bring a repository you want to contribute to.'],['GitHub Data Collection',Database,'The backend retrieves issues, code metadata, and project signals.'],['Repository Mining',Map,'It maps files, modules, dependencies, and history.'],['Knowledge Graph',BrainCircuit,'Relationships turn raw repository context into an explorable map.'],['Difficulty / Skill / Impact Analysis',Sparkles,'The backend evaluates what an issue needs and what it affects.'],['Contribution Path',Route,'ContriMap presents a practical path from investigation to pull request.'],['AI Explanation',BrainCircuit,'Backend-generated guidance explains the recommendation in plain language.']]
export default function HowItWorks(){return <><Navbar/><main className="mx-auto max-w-3xl px-5 py-20"><p className="eyebrow">A transparent workflow</p><h1 className="mt-3 text-4xl font-extrabold">From repository URL to a clear next step.</h1><p className="mt-5 max-w-2xl leading-7 text-slate-600">ContriMap is a frontend for the analysis pipeline—not the analysis engine itself. GitHub data and AI insights are processed by the connected backend, then presented as a contributor-friendly workspace.</p><div className="mt-12">{steps.map(([title,Icon,text],i)=><div key={title}><div className="card flex gap-5 p-6"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-sky text-blue-700"><Icon size={21}/></span><div><p className="text-xs font-bold text-mint">STEP {i+1}</p><h2 className="mt-1 font-extrabold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{text}</p></div></div>{i<steps.length-1&&<ArrowDown className="mx-auto my-2 text-mint"/>}</div>)}</div></main></>}
