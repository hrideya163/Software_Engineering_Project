import { useEffect } from 'react'; import { getRepository } from '../api/repositoryApi'; import { getIssues } from '../api/issueApi'; import { useApp } from '../context/AppContext'
export function useRepository(owner, repo) { const app = useApp(); useEffect(() => { let active = true; app.setLoading(true); app.setError(''); Promise.all([getRepository(owner, repo), getIssues(owner, repo)]).then(([repository, issues]) => { if(active) { app.setCurrentRepository(repository); app.setIssues(issues) } }).catch(err => active && app.setError(err.message || 'Unable to load this repository.')).finally(() => active && app.setLoading(false)); return () => { active = false } }, [owner, repo]); return app }
